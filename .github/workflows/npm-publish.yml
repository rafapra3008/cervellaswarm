# DEPRECATED: Consolidated into publish.yml (which has CI gate + environment protection)
#
# This workflow was disabled in S443 because it conflicted with publish.yml:
# both triggered on tag push and both tried to publish CLI to npm.
# The second publish would fail with "version already exists".
#
# publish.yml now handles CLI + MCP Server with proper CI gate.
#
# Original SETUP notes preserved:
# 1. Go to npmjs.com/package/cervellaswarm/access
# 2. Add Trusted Publisher → GitHub Actions
# 3. Owner: rafapra3008
# 4. Repository: CervellaSwarm
# 5. Workflow: publish.yml (NOT this file)
# 6. Environment: production

name: "[DEPRECATED] npm-publish"

# Trigger disabled - use publish.yml instead
on:
  workflow_dispatch:
    inputs:
      note:
        description: "DEPRECATED: Use publish.yml instead"
        required: false
