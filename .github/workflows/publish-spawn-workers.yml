# Publish cervellaswarm-spawn-workers to PyPI
# Triggered on tag: spawn-workers-v*
# Trusted Publisher config on PyPI: workflow=publish-spawn-workers.yml, environment=pypi
# NOTE: Publish/release jobs inline due to PyPI OIDC limitation (warehouse#11096)

name: Publish spawn-workers

on:
  push:
    tags:
      - 'spawn-workers-v[0-9]+.[0-9]+.[0-9]+'
      - 'spawn-workers-v[0-9]+.[0-9]+.[0-9]+-*'

concurrency:
  group: publish-spawn-workers-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    uses: ./.github/workflows/_build-package.yml
    with:
      package_name: "spawn-workers"
      package_dir: "packages/spawn-workers"

  publish-testpypi:
    name: Publish to TestPyPI
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha')
    environment:
      name: testpypi
      url: https://test.pypi.org/p/cervellaswarm-spawn-workers
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: spawn-workers-distributions
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true
          verbose: true

  publish-pypi:
    name: Publish to PyPI
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: "!contains(github.ref_name, '-rc') && !contains(github.ref_name, '-beta') && !contains(github.ref_name, '-alpha')"
    environment:
      name: pypi
      url: https://pypi.org/p/cervellaswarm-spawn-workers
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: spawn-workers-distributions
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          verbose: true

  github-release:
    name: Create GitHub Release
    needs: [build, publish-pypi, publish-testpypi]
    if: always() && needs.build.result == 'success' && (needs.publish-pypi.result == 'success' || needs.publish-testpypi.result == 'success')
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v8
        with:
          name: spawn-workers-distributions
          path: dist/
      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#spawn-workers-v}" >> "$GITHUB_OUTPUT"
      - uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          name: "spawn-workers ${{ steps.version.outputs.version }}"
          body: |
            ## CervellaSwarm spawn-workers ${{ steps.version.outputs.version }}

            **Process management for AI agent teams. tmux/nohup backends.**

            **Install:**
            ```bash
            pip install cervellaswarm-spawn-workers==${{ steps.version.outputs.version }}
            ```

            **What's new:**
            - See [CHANGELOG](https://github.com/rafapra3008/cervellaswarm/blob/${{ github.ref_name }}/packages/spawn-workers/CHANGELOG.md)

            **Links:**
            - [PyPI package](https://pypi.org/project/cervellaswarm-spawn-workers/${{ steps.version.outputs.version }}/)
            - [Documentation](https://github.com/rafapra3008/cervellaswarm/tree/main/packages/spawn-workers#readme)

            ---
            *Part of the CervellaSwarm multi-agent framework.*
          files: dist/*
          draft: false
          prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') }}
