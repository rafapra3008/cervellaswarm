# CI Workflow for CervellaSwarm CLI
#
# Runs on every push to main and on PRs.
# Jobs: lint -> test (matrix) -> build-check
#
# Quality is not negotiable.

name: CI

on:
  push:
    branches: [main]
    paths:
      - 'packages/cli/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'packages/cli/**'

# Cancel in-progress runs on same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'packages/cli/package-lock.json'

      - name: Install and build core dependency
        run: npm ci && npm run build
        working-directory: packages/core

      - name: Install dependencies
        run: npm ci
        working-directory: packages/cli

      - name: Run ESLint
        run: npm run lint
        working-directory: packages/cli

  test:
    name: Test (Node ${{ matrix.node-version }})
    needs: [lint]
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: 'packages/cli/package-lock.json'

      - name: Install and build core dependency
        run: npm ci && npm run build
        working-directory: packages/core

      - name: Install dependencies
        run: npm ci
        working-directory: packages/cli

      - name: Run tests
        run: npm test
        working-directory: packages/cli

      - name: Generate coverage (Node 20 only)
        if: matrix.node-version == '20.x'
        continue-on-error: true
        run: npm run test:coverage
        working-directory: packages/cli

  build-check:
    name: Build & Package Test
    needs: [test]
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'packages/cli/package-lock.json'

      - name: Install and build core dependency
        run: npm ci && npm run build
        working-directory: packages/core

      - name: Install dependencies
        run: npm ci
        working-directory: packages/cli

      - name: Test package build
        run: npm pack
        working-directory: packages/cli

      - name: Install core and CLI globally
        run: |
          cd packages/core && npm pack && npm install -g *.tgz && cd ../..
          cd packages/cli && npm install -g *.tgz
        working-directory: .

      - name: Verify CLI works
        run: |
          cervellaswarm --version
          cervellaswarm --help

      - name: Upload package artifact
        uses: actions/upload-artifact@v7
        with:
          name: npm-package
          path: packages/cli/*.tgz
          retention-days: 90
