# Publish Workflow for cervellaswarm-code-intelligence (PyPI)
#
# Triggered on tags matching 'code-intelligence-v*'
# Uses Trusted Publishing (OIDC) - no API tokens needed!
#
# Setup required on PyPI:
#   1. Go to pypi.org -> Your projects -> Publishing
#   2. Add new pending publisher:
#      - Owner: rafapra3008
#      - Repository: cervellaswarm
#      - Workflow: publish-pypi.yml
#      - Environment: pypi (for production)
#   3. Same for test.pypi.org with environment: testpypi
#
# Quality is not negotiable.

name: Publish to PyPI

on:
  push:
    tags:
      - 'code-intelligence-v[0-9]+.[0-9]+.[0-9]+'
      - 'code-intelligence-v[0-9]+.[0-9]+.[0-9]+-*'

# NEVER cancel publish in-flight!
concurrency:
  group: publish-pypi-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ---------------------------------------------------------------
  # Step 1: Build and verify the package
  # ---------------------------------------------------------------
  build:
    name: Build & Verify
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install build tools
        run: python -m pip install --upgrade pip build twine

      - name: Build package
        run: python -m build
        working-directory: packages/code-intelligence

      - name: Verify package metadata
        run: twine check dist/*
        working-directory: packages/code-intelligence

      - name: Verify wheel contents
        run: |
          echo "=== Wheel contents ==="
          python -c "
          import zipfile, os
          whl = [f for f in os.listdir('dist') if f.endswith('.whl')][0]
          with zipfile.ZipFile(f'dist/{whl}') as z:
              for name in sorted(z.namelist()):
                  print(f'  {name}')
          "
          echo ""
          echo "=== Package version ==="
          python -c "
          import zipfile, os
          whl = [f for f in os.listdir('dist') if f.endswith('.whl')][0]
          with zipfile.ZipFile(f'dist/{whl}') as z:
              meta = [n for n in z.namelist() if 'METADATA' in n][0]
              for line in z.read(meta).decode().splitlines():
                  if line.startswith(('Name:', 'Version:', 'License')):
                      print(line)
          "
        working-directory: packages/code-intelligence

      - name: Run tests
        run: |
          pip install -e ".[test]"
          pytest tests/ -q --tb=short
        working-directory: packages/code-intelligence

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: packages/code-intelligence/dist/
          retention-days: 30

  # ---------------------------------------------------------------
  # Step 2: Publish to TestPyPI (pre-releases and release candidates)
  # ---------------------------------------------------------------
  publish-testpypi:
    name: Publish to TestPyPI
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha')

    environment:
      name: testpypi
      url: https://test.pypi.org/p/cervellaswarm-code-intelligence

    permissions:
      id-token: write

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true
          verbose: true

  # ---------------------------------------------------------------
  # Step 3: Publish to PyPI (stable releases only)
  # ---------------------------------------------------------------
  publish-pypi:
    name: Publish to PyPI
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: "!contains(github.ref_name, '-rc') && !contains(github.ref_name, '-beta') && !contains(github.ref_name, '-alpha')"

    environment:
      name: pypi
      url: https://pypi.org/p/cervellaswarm-code-intelligence

    permissions:
      id-token: write

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          verbose: true

  # ---------------------------------------------------------------
  # Step 4: Create GitHub Release (runs after either PyPI or TestPyPI)
  # ---------------------------------------------------------------
  github-release:
    name: Create GitHub Release
    needs: [build, publish-pypi, publish-testpypi]
    if: always() && needs.build.result == 'success' && (needs.publish-pypi.result == 'success' || needs.publish-testpypi.result == 'success')
    runs-on: ubuntu-latest
    timeout-minutes: 5

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#code-intelligence-v}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "Code Intelligence ${{ steps.version.outputs.version }}"
          body: |
            ## CervellaSwarm Code Intelligence ${{ steps.version.outputs.version }}

            **Install:**
            ```bash
            pip install cervellaswarm-code-intelligence==${{ steps.version.outputs.version }}
            ```

            **What's new:**
            - See [CHANGELOG](https://github.com/rafapra3008/cervellaswarm/blob/${{ github.ref_name }}/packages/code-intelligence/CHANGELOG.md)

            **Links:**
            - [PyPI package](https://pypi.org/project/cervellaswarm-code-intelligence/${{ steps.version.outputs.version }}/)
            - [Documentation](https://github.com/rafapra3008/cervellaswarm/tree/main/packages/code-intelligence#readme)

            ---
            *AST-based code analysis: symbol extraction, dependency graphs, semantic search.*
          files: dist/*
          draft: false
          prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') }}
