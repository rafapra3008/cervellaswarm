# CervellaSwarm Dashboard API - Requirements
# Python >= 3.10

# FastAPI framework
fastapi>=0.109.0

# ASGI server
uvicorn[standard]>=0.27.0

# Pydantic per validazione
pydantic>=2.0.0

# Markdown parsing (opzionale, per parsing avanzato)
python-frontmatter>=1.0.0

# File watching per SSE (opzionale, per watchdog invece di polling)
watchdog>=3.0.0
