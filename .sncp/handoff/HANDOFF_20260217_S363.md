# HANDOFF - Sessione 363 (17 Febbraio 2026)

> **Tema:** FASE 0 Open Source - Sicurezza repository
> **Durata:** ~3h (incluso auto-compact)
> **Score Guardiana:** 9.3/10

---

## COSA ABBIAMO FATTO

### 1. Audit Completo del Codebase

Abbiamo lanciato 2 agenti in parallelo per mappare TUTTI i dati personali nel repo:

**Ingegnera** ha trovato:
- 848 occorrenze di `rafapra` in 342 file
- 781 occorrenze di `/Users/rafapra/` in 306 file
- La maggior parte (>200 file) sono in directory gia escluse (.swarm/, reports/, logs/)
- `packages/` e PULITO: zero dati personali
- Report completo con 4 tabelle: file privati, paths hardcoded, riferimenti progetti, file gia puliti
- Stima totale F0: 139 file da sanitizzare, 16-22h di lavoro

**Security** ha trovato:
- Score: 4/10 privato, 8/10 pubblico (il public e gia protetto dalla whitelist)
- 21 finding: 1 CRITICAL (Stripe ID in .swarm), 8 HIGH, 8 MEDIUM, 4 LOW
- Report: `.sncp/progetti/cervellaswarm/reports/SECURITY_20260217.md`

### 2. .gitignore Hardening

Aggiornato .gitignore con 10+ nuovi pattern. Poi `git rm --cached` su **1006 file** che non devono essere nel tracking git. I file RESTANO SU DISCO - zero impatto su La Famiglia.

Nuovi pattern aggiunti:
- `.swarm/` (era parziale, ora tutto)
- `data/` (database, retro reports, logs operativi)
- `logs/`, `reports/` (dati runtime)
- `.mcp.json` (config con path locali)
- `.vscode/` (IDE config con path)
- `config/claude-hooks/` (settings personali)
- `cervellaswarm-extension/` (VS Code extension con config)
- `RESEARCH_*.md`, `RICERCA_*.md`, `PASSAGGIO_CONSEGNA_*.txt`
- `*.db`, `*.sqlite`

### 3. sync-to-public.sh v3.0

Lo script che sincronizza dal repo privato al pubblico e stato potenziato:

- **NUOVO Check 5: Content Scanning** - scansiona il CONTENUTO dei file (non solo nomi) per 14 pattern sensibili
- Pattern: `/Users/rafapra`, `rafapra3008`, `~/Developer/`, `@gmail.com`, `192.168.`, `acct_1`, `data-frame-476309`, tutti i nomi di progetti privati (miracollogeminifocus, ContabilitaAntigravity, CervellaBrasil, chavefy, cervellacostruzione, contabilita)
- Blacklist root espansa: +`.swarm`, `.mcp.json`, `logs`, `config`, `cervellaswarm-extension`
- Blacklist filename patterns: +`RESEARCH_`, `RICERCA_`, `PASSAGGIO_CONSEGNA`, `HANDOFF_`, `CREDENZIALI`
- `--dry-run` per preview senza push

### 4. Community Files

Creati i file standard per open source:
- **CODE_OF_CONDUCT.md** - Contributor Covenant 2.1, contact: cervellaswarm@pm.me
- **SECURITY.md** - Responsible disclosure, timeline (48h ack, 1 week assessment)
- LICENSE (Apache 2.0), CONTRIBUTING.md (DCO), NOTICE - gia esistenti e corretti

### 5. Fix Sicurezza Chirurgici

- `.claude/settings.json`: path `/Users/rafapra/...` hardcoded -> `$CLAUDE_PROJECT_DIR/...`
- `docs/SEMANTIC_SEARCH.md`: 14 occorrenze path personali -> `/path/to/your-project`
- `docs/GETTING_STARTED.md`: 2 occorrenze path personali -> `~/your-project`
- `docs/DUAL_REPO_STRATEGY.md`: whitelist section aggiornata con +3 file nuovi

### 6. Audit Guardiana (3 round)

| Round | Cosa | Score | P2 trovati | Risolti? |
|-------|------|-------|------------|----------|
| 1 | .gitignore | 8.8/10 | 3 (data/retro, .vscode, settings hardcoded) | Si |
| 2 | Finale S363 | 9.3/10 | 2 (SEMANTIC_SEARCH + GETTING_STARTED self-blocking) | Si |
| 3 | Content scanner fix | N/A | Aggiunto rafapra3008 + project names | Si |

---

## DECISIONI PRESE (con PERCHE)

| Decisione | Perche |
|-----------|--------|
| **Whitelist approach** (non ristrutturare repo) | La Famiglia resta identica localmente, zero rischio di rompere workflow |
| **Content scanning** nel sync script | Defense-in-depth: anche se qualcuno sbaglia la whitelist, il contenuto viene verificato |
| **git rm --cached** (non delete) | File restano su disco, git li ignora. Zero impatto operativo |
| **.gitignore copre runtime data** | data/, logs/, reports/ sono dati operativi, non codice da versionare |
| **14 pattern** nel content scanner | Copre: path personali, username GitHub, email, IP, nomi di TUTTI i progetti privati |

---

## STATO GIT

3 commit pushati in S363:
```
82bd373 Fix: sync-to-public content scanner - add rafapra3008 + project names
669d862 checkpoint(S363): FASE 0 Open Source - sanitize docs + PROMPT_RIPRESA
0f0580b ANTI-COMPACT: PreCompact auto (bulk: .gitignore, 1006 untrack, sync v3.0, CODE_OF_CONDUCT, SECURITY)
```

Branch: `main`, up to date con `origin/main`.

---

## COSA MANCA PER COMPLETARE FASE 0

L'Ingegnera ha mappato tutto. Ecco il piano dettagliato:

### F0.4 - Sanitizzare scripts con path hardcoded (~3-4h)
- 25 script bash con `/Users/rafapra/` hardcoded
- Pattern da usare: `${CERVELLASWARM_ROOT:-$(git rev-parse --show-toplevel)}`
- Focus: `scripts/cron/` (6 file), `scripts/sncp/` (8 file), `scripts/start-session.sh`

### F0.5 - Sanitizzare packages/ URLs (~1h)
- 12 file con `github.com/rafapra3008` -> URL repo pubblico
- `packages/mcp-server/src/sncp/reader.ts` -> KNOWN_PROJECTS da rendere configurabile

### F0.6 - README.md killer per repo pubblico (~2h)
- Hero section, feature highlights, badges
- Quick start con esempi reali
- Comparison table vs competitor (AutoGen, CrewAI, LangGraph)

### F0.7 - .github/ templates (~1h)
- Issue templates (bug report, feature request)
- PR template
- CI/CD base (lint, test)

### F0.8 - Content scanner esteso (quick)
- Aggiungere `*.html`, `*.css`, `*.txt` ai file scansionati

### Stima totale F0 rimanente: ~8-10h (~2 sessioni)

---

## PRINCIPIO GUIDA

**"Attenzione per non toccare nulla che possa fare qualcosa per noi"** - Rafa, S363

La Famiglia (17 agenti) continua a funzionare ESATTAMENTE come prima. Il repo pubblico e una PROIEZIONE curata del privato, non una ristrutturazione. Zero rischio.

---

*"Lavoriamo in pace! Senza casino! Dipende da noi!"*
*Cervella & Rafa - S363*
