__pycache__/
*.egg-info/
dist/
build/
.DS_Store
.playwright-mcp/

# Agent worktree temp directories
tmp*/
cervella_reg_*/
pytest-of-*/
.claude/worktrees/

# Per-machine Claude Code local settings (untracked S542, by-design upstream)
.claude/settings.local.json

# Data directory (operational data, logs, database, retro reports)
data/
!packages/lingua-universale/src/cervellaswarm_lingua_universale/stdlib/data/
*.db
*.sqlite
# S633 (gate Qualita', P3-3): `*.db` copre il database ma NON i suoi sidecar SQLite.
# Effetto: bastava lanciare pytest o swarm-doctor dalla root perche' `.cervella/` comparisse
# come non tracciata -- rumore su un albero dove il dirty ALTRUI e' un pericolo vivo (D4).
*.db-wal
*.db-shm

# Environment
.env
*.env
.venv/
venv/

# Swarm operational data (personal data, IPs, emails, infra details)
# S363: entire .swarm/ has sensitive data in handoff, security, tasks
.swarm/
CREDENZIALI_OAUTH.md

# Logs and reports (hardcoded paths and operational data)
logs/
reports/

# MCP config (local paths)
.mcp.json

# MCP publisher tokens (S466: mcp-publisher login creates these)
.mcpregistry_*

# IDE config (personal paths)
.vscode/

# Personal research files at root
RESEARCH_*.md
RICERCA_*.md
PASSAGGIO_CONSEGNA_*.txt

# Node modules
packages/*/node_modules/
node_modules/

# Coverage reports
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.pytest_cache/

# Private files (NEVER sync to public)
.sncp/
# S519 D3 P4.5 NC5: split_prompt_ripresa audit log (already covered by .sncp/
# wildcard above; explicit pattern documents intent for future migrations).
.sncp/reports/split_log_*.md
NORD.md
docs/studio/

# VS Code extension with personal config
cervellaswarm-extension/

# VHS generated video files (regenerate from .tape source files)
packages/lingua-universale/demo/*.gif
packages/lingua-universale/demo/*.mp4

# Backup files pattern (riusabile per ogni S### session)
# S517: cattura *.sNNN-*-pre-edit-*, *.sNNN-dN-*, *.sNNN-pre-rotation-*
*.s[0-9][0-9][0-9]-*-pre-edit*
*.s[0-9][0-9][0-9]-pre-edit*
*.s[0-9][0-9][0-9]-d[0-9]-*
*.s[0-9][0-9][0-9]-pre-rotation-*
*.bak-s[0-9][0-9][0-9]-*
