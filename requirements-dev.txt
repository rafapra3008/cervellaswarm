# CervellaSwarm Development Dependencies (Optional)
#
# Queste dipendenze sono OPZIONALI.
# Gli script funzionano anche senza rich/pytest, con fallback su plain text.

# Rich - Terminal output formattato (tabelle, panel, progress bar)
# Usato da: analytics/cli.py, retro/cli.py, analyze_codebase.py, wizard.py
rich>=13.0.0

# Pytest - Testing framework
# Usato per: test suite completa
pytest>=7.0.0
pytest-cov>=4.0.0

# Ruff - Fast Python linter
# Usato per: code quality checks
ruff>=0.1.0

# Tree-sitter - AST parsing multi-linguaggio
# Usato da: treesitter_parser.py, symbol_extractor.py
tree-sitter>=0.20.0
tree-sitter-language-pack>=0.1.0

# NetworkX + NumPy - Graph analysis per dependency ranking
# Usato da: dependency_graph.py (PageRank). NumPy needed by networkx.pagerank().
networkx>=3.0
numpy>=1.24.0
